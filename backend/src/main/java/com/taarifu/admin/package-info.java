/**
 * admin module — <b>not implemented this increment</b> (ARCHITECTURE.md §3.1, FOUNDATION-SCOPE.md §1).
 *
 * <p>Planned responsibility: Reference-data administration, taxonomy/SLA config, role granting, feature flags/AppConfig, bulk import. This package exists now only to <b>lock the canonical layout</b>;
 * it holds no code until that module's increment.</p>
 */
package com.taarifu.admin;
