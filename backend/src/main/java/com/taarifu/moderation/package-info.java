/**
 * moderation module — <b>not implemented this increment</b> (ARCHITECTURE.md §3.1, FOUNDATION-SCOPE.md §1).
 *
 * <p>Planned responsibility: Flag, ModerationCase, Appeal, content-safety pipeline, operator-assisted verification review; ContentSafety port. This package exists now only to <b>lock the canonical layout</b>;
 * it holds no code until that module's increment.</p>
 */
package com.taarifu.moderation;
