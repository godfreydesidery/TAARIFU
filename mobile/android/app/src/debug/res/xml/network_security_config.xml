<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG-ONLY network security config.

  WHY: the default dev base URL is http://10.0.2.2:8081 (the Android emulator's
  alias for the host's localhost). Android API 28+ blocks cleartext (HTTP) by
  default, which would make `flutter run` against a local backend fail. This
  permits cleartext to the dev hosts ONLY, and lives under src/debug so it is
  NOT bundled into release builds — production must use HTTPS (PRD §18).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
