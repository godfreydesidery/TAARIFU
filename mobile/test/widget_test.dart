/// Smoke test placeholder replaced by focused unit/bloc tests:
///   * `api_response_test.dart` — envelope + error decoding.
///   * `auth_bloc_test.dart` — onboarding state machine.
///
/// This file is intentionally a no-op so the default scaffolded widget test does
/// not run against the full app (which needs network/secure-storage plugins
/// unavailable in the plain VM test host).
library;

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — see api_response_test.dart and auth_bloc_test.dart', () {
    expect(true, isTrue);
  });
}
